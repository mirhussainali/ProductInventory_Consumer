---
swagger: "2.0"
info:
  description: "API to lookup all Products or specific Product by ID"
  version: "1.0"
  title: "ProductsAPI"
host: "webm1015.eastus.cloudapp.azure.com:5555"
basePath: "/rad/sbux_provider.localapi:sbuxproductsapi"
schemes:
- "http"
- "https"
consumes:
- "application/json"
produces:
- "application/json"
paths:
  /getProductByID:
    get:
      operationId: "getProductByID_GET_1"
      parameters:
      - name: "productID"
        in: "query"
        required: true
        type: "string"
      responses:
        "200":
          description: "OK"
          schema:
            $ref: "#/definitions/getProductByID_GET_response"
        "401":
          description: "Access Denied"
  /getAllProducts:
    get:
      operationId: "getAllProducts_GET_2"
      parameters: []
      responses:
        "200":
          description: "OK"
          schema:
            $ref: "#/definitions/getAllProducts_GET_response"
        "401":
          description: "Access Denied"
securityDefinitions:
  BasicAuth:
    description: "Basic Auth"
    type: "basic"
definitions:
  productDetail:
    required:
    - "activeInd"
    - "countryCode"
    - "name"
    - "price"
    - "productId"
    - "productType"
    - "publishStatus"
    properties:
      productId:
        type: "integer"
        format: "int32"
      price:
        type: "string"
      activeInd:
        type: "string"
      countryCode:
        type: "string"
      name:
        type: "string"
      productType:
        type: "string"
      publishStatus:
        type: "string"
  ProductInventoryJSONDT:
    required:
    - "id"
    - "productDetail"
    - "publishTimestamp"
    properties:
      productDetail:
        $ref: "#/definitions/productDetail"
      publishTimestamp:
        type: "string"
      id:
        type: "string"
    description: "generated by webMethods"
  getProductByID_GET_response:
    required:
    - "productDetails"
    properties:
      productDetails:
        type: "array"
        items:
          $ref: "#/definitions/ProductInventoryJSONDT"
  getAllProducts_GET_response:
    required:
    - "productDetails"
    properties:
      productDetails:
        type: "array"
        items:
          $ref: "#/definitions/ProductInventoryJSONDT"
